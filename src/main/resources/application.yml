server:
  port: 8081
  servlet:
    contextPath: '/global'

microservice:
  contextPath: '/api/v1/scm-app'

spring:
  application:
    name: scm-backend
#  datasource:
#    driverClassName:
#    url:
#    username:
#    password:
#  jpa:
#    generate-ddl:
#    database-platform: org.hibernate.dialect.MySQL
#    hibernate.ddl-auto: none
#    properties.hibernate.temp.use_jdbc_metadata_defaults: false
#    properties.hibernate.jdbc.batch_size: 25
#hibernate.hbm2ddl.auto:

spring.datasource.url: jdbc:mysql://localhost:3306/mdafjaldb
spring.datasource.username: root
spring.datasource.password: password
spring.datasource.driver-class-name: com.mysql.cj.jdbc.Driver
  
  #hibernate configuration
spring.jpa.hibernate.ddl-auto: update
#create-drop, validate, create, none, update
spring.jpa.show-sql: true
spring.jpa.properties.hibernate.dialect: org.hibernate.dialect.MySQL8Dialect
spring.jpa.properties.hibernate.format_sql: true
